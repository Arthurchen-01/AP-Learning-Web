# Chrome 插件当前进度交接

更新时间：2026-03-27 21:24
用途：给另一个 AI 直接接手当前状态，基于已读过的 `协作分工说明.md` 继续并行推进，并保证后续成果能和我这边已完成内容顺利合并。

---

## 一、当前总目标

项目目标不变：
做一个 **AP 模考专项采集 Chrome/Edge 扩展**，进入目标考试页面后，自动采集：
- fetch / XHR 请求与响应
- localStorage / sessionStorage /（后续）IndexedDB
- 页面注水状态
- 关键交互事件（start/resume/next/flag/review/module 等）
- 后续再补 DOM / 资源 / HAR / CSS / 工具栏规则层

当前优先不是做“最完美版本”，而是：
**尽快做出一个能跑的 MVP，然后你我两边再合并增强。**

---

## 二、我这边已经完成到哪里了

### 1. 协作分工文档已完成
你已经读过：
- `C:\Users\25472\Desktop\chrome插件\协作分工说明.md`

所以这里不重复讲分工原则，只讲现在的实际进度。

### 2. 已创建插件项目骨架
项目目录：
- `C:\Users\25472\projects\methods\chrome-ap-capture`

当前已存在文件：
- `manifest.json`
- `README.md`
- `src\shared\config.js`
- `src\background\service-worker.js`
- `src\content\injected-hook.js`
- `src\content\content.js`
- `src\options\options.html`
- `src\options\options.js`

### 3. 已完成的能力（MVP 第一版）
#### 扩展基础
- MV3 扩展骨架已建好
- background service worker 已接消息
- options 页面已能保存基础配置

#### 网络采集
- 已在页面层 monkey patch：
  - `fetch`
  - `XMLHttpRequest`
- 已能采集：
  - request URL
  - method
  - request body（基础版）
  - response status
  - responseText（基础版，带截断）
  - startedAt / finishedAt

#### 页面状态采集
- 已能采集：
  - `localStorage`
  - `sessionStorage`
  - 注水状态：
    - `__NEXT_DATA__`
    - `__INITIAL_STATE__`
    - `__NUXT__`

#### 事件采集
- 已实现基础点击识别：
  - `start`
  - `resume`
  - `next`
  - `previous`
  - `flag`
  - `review`
  - `module`
  - `directions`
- 点击这些按钮时会尝试做 snapshot 并下载 JSON

#### 下载输出
- 已接入 `chrome.downloads.download`
- 当前会把 JSON 直接下载到浏览器默认下载目录中，以 `sessionId/type/timestamp_*.json` 的结构组织

---

## 三、我刚做过的重要收敛（和目标站相关）

### 1. 目标网站已明确
目标不是泛化的任意考试网站，而是优先适配：
- `https://mokaoai.com/en/ap/1902622417307406336`
- `https://mokaoai.com/en/ap/exam?examId=1902622417307406336&timekeepingModeOn=true&type=0`

### 2. 已把插件自动识别规则改为偏向 mokaoai.com
我已经修改了：
- `src\shared\config.js`
- `src\content\content.js`

当前规则会优先匹配：
- `mokaoai.com/en/ap/`
- `mokaoai.com/en/ap/exam`
- `examId=`
- `timekeepingModeOn=`
- `targetOrigins = ['https://mokaoai.com']`

也就是说，现在插件不会像之前那样主要按“泛化关键词”乱匹配，而是开始针对你这个站工作。

### 3. 本地已有原始 JSON 已确认存在
路径：
- `C:\Users\25472\projects\methods\mokaoai.com\database\01_raw\json`

并且已确认 examId：
- `1902622417307406336`

对应本地已有文件：
- `CSA__AP CSA ...__1902622417307406336.json`
- 还有一个 `(1)` 重复副本

这很关键：
**说明插件输出最终可以和你本地 raw json 对照，不是孤立数据。**

### 4. 已确认目标页面结构
我已经打开目标页面看过，当前页面是：
- `Choose a Full-Length Practice`
- 可见控件包括：
  - Test Type 三种 radio
  - Timer Mode 开关
  - `Resume` 按钮

这意味着后续事件识别应该重点适配这些交互，不必再盲猜。

---

## 四、当前代码状态的不足 / 未完成点

以下是当前 MVP 第一版还明显不够的地方：

### 1. 还没有 manifest / timeline 聚合文件
当前是每个 event / network 项各自下载成 JSON，**但还没有统一的**：
- `session-manifest.json`
- `timeline.json`

这会影响后续分析和合并。

### 2. 事件识别还是基于按钮文本，比较粗
当前逻辑主要是：
- 抓点击事件
- 看按钮文字包含 `start/resume/next/...`

缺点：
- 对中文按钮、图标按钮、aria-label 变化不稳
- 对 radio / switch 的配置变更支持弱

### 3. 还没有 storage diff
现在只有全量快照，没有：
- localStorage diff
- sessionStorage diff

### 4. IndexedDB 还没做
完全没做，属于当前最明显缺口之一。

### 5. DOM / 资源 / CSS 还很弱
当前只有非常粗糙的 `domSummary()`：
- title
- url
- 前 80 个按钮文本
- hasQuestionLikeText

还没有真正的：
- outerHTML 快照
- 关键组件 DOM
- CSS 资源清单
- 图片 / 图标 / svg 资源索引

### 6. 还没做实际加载测试
扩展骨架虽然已经写完，但我还没真正做“浏览器加载扩展并走完整目标路径”的实测闭环。

---

## 五、你现在最适合接手做什么

根据当前状态，你最适合接的是**增强层和可合并层**，避免和我重复写骨架。

### 你优先做的事（按优先级）

#### A. 设计/补实现 session 聚合结构
请先做一套能与当前输出合并的聚合方案：
- `session-manifest.json`
- `timeline.json`
- 事件索引
- 网络索引
- 快照索引

要求：
- 能兼容我当前每次下载一个 JSON 的模式
- 未来也能兼容后续打包器/zip 方案

建议输出结构示例：
```json
{
  "sessionId": "...",
  "startedAt": "...",
  "targetUrl": "...",
  "events": [...],
  "networkFiles": [...],
  "snapshotFiles": [...],
  "notes": []
}
```

#### B. 设计并实现 storage diff 方案
请重点补：
- localStorage diff
- sessionStorage diff
- 快照前后 key 变化记录

要求：
- 结果能直接嵌进我现有 snapshot 输出
- 不要推翻现有 snapshot 格式，尽量是增量兼容

#### C. 补 IndexedDB 方案（非常重要）
你负责把这块补出来：
- 如何枚举 DB
- 如何导出 object stores
- 如何把导出结果写成 JSON
- 如何在 event 触发时做快照

要求：
- 先给出能落地的 MVP 版本
- 不要只停留在概念文档
- 结果最好能和 `storage` 一起归档到统一结构下

#### D. 设计 DOM / 资源采集增强版
请补方案并最好直接产出实现建议：
- 关键页面 outerHTML
- 关键容器 selector 的 HTML 快照
- CSS / JS / image 资源索引
- 工具栏可见性抓取

目标：
让后续能更好复刻界面，而不仅仅是拿到数据接口。

#### E. 继续补开源调研（若还有余力）
- 哪些仓库最值得直接借鉴结构
- 哪些 IndexedDB 导出思路靠谱
- 哪些 HAR/session 归档方式适合浏览器扩展

---

## 六、你不要做什么（避免打架）

请尽量不要重复做这些内容：
- 不要从零重建 manifest / background / content / options 骨架
- 不要重写我现在已有的 fetch/XHR 基础 patch 逻辑，除非你发现明显 bug 并准备兼容式修复
- 不要把目标站又泛化回“所有考试站”，现在先优先服务 `mokaoai.com`

如果你要改现有结构，请遵守：
- **增量兼容**
- 尽量新增文件或新增模块，而不是整体推翻

---

## 七、和我现有代码合并时的要求

### 1. 保持当前目录结构兼容
当前目录：
- `src\background`
- `src\content`
- `src\options`
- `src\shared`

你新增代码最好也沿这个思路：
- 如果是 storage diff / indexeddb，可考虑：
  - `src\shared\storage-utils.js`
  - `src\content\indexeddb-capture.js`
  - 或类似增量文件

### 2. 配置尽量继续走 `DEFAULT_CONFIG`
当前：
- `src\shared\config.js`

新增能力最好以可配置项扩展进去，例如：
- `captureIndexedDB: true`
- `captureDomHtml: true`
- `captureResources: true`
- `maxIndexedDbRows: 1000`

### 3. 输出 JSON 尽量兼容当前字段
当前已有顶层对象思路：
```json
{
  "sessionId": "...",
  "capturedAt": "...",
  "eventType": "...",
  "url": "...",
  "storage": {},
  "injectedState": {},
  "dom": {},
  "extra": {}
}
```

你新增字段时，尽量增量加：
- `storageDiff`
- `indexedDb`
- `resources`
- `toolState`
- `selectors`

### 4. 最好产出“可直接合并”的东西
理想交付是以下二选一：

#### 方案 1：直接代码
你直接给出：
- 新增哪些文件
- 如何 import
- 如何在现有 `content.js` / `service-worker.js` 里接入

#### 方案 2：清晰到能秒接的 patch 方案
如果不直接写代码，也请至少写成：
- 修改哪个文件
- 替换哪段
- 新增哪段
- 新增文件内容

不要只写抽象建议。

---

## 八、当前我这边下一步准备做的事（你需要知道）

我这边接下来大概率会继续推进：
1. 调整事件识别，让它更贴合 `Resume` / `Timer Mode` / exam 页面真实交互
2. 做基础 manifest / timeline 输出
3. 准备浏览器实测闭环

所以你如果并行推进，最好避开与我这三块的强冲突。

---

## 九、你现在最该输出什么

如果你想最有效帮忙，请优先交付：

### 第一优先级
- IndexedDB 采集实现方案（最好带代码）
- storage diff 设计 + 可接入代码
- session manifest / timeline 结构设计

### 第二优先级
- DOM / 资源采集增强方案（最好带 selector 粒度建议）

### 第三优先级
- HAR / screenshot / console / cookie 增强建议

---

## 十、一句话总结

**当前插件骨架和基础 fetch/XHR + storage + injected state 抓取已经完成，目标网站已明确锁定为 mokaoai.com 的两个 AP 路由，examId 对应本地 raw json 已确认存在。你现在最适合接手的是：session 聚合、storage diff、IndexedDB、DOM/资源增强，且必须以增量兼容方式设计，保证能直接并回现有代码。**
