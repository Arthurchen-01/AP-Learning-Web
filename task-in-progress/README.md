# task-in-progress

Use `task-status.json` to track the currently active task.

- Set `active=true` when a task starts.
- Update `summary`, `confidence`, and `nextStep` as work progresses.
- Set `active=false` when the task is completed or paused.
